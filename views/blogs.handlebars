<section class="container">
  <h3 class="gallery-name">{{blog.title}}</h3>
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each blog.blogger as |blogger|}}
    {{> blogger-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}